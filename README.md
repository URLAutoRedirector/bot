# Bot for URL Auto Redirector

Build packaged `.zip` file with the latest version:
```
$ rake release:build
```

Create and push a tag:
```
$ rake release:tag
```

Update changelog:
```
$ rake release:changelog
```
